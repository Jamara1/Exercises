let people = ['Fernando', 'Johan', 'Francisco'];
let salarys = [1000, 1200, 3000];

for (let i = 0; i < people.length; i++) {
    const element = people[i];
    console.log(element + ' have salary the ' + salarys[i]);
}