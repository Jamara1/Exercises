let people = {
    name: 'Johan',
    age: 22,
    height: 1.79
};

function describePerson(person) {
    console.log(`${person.name} have ${person.age} years old and is ${person.height} meters tall`);
}

describePerson(people);