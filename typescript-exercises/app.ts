let message: string = 'Hello!';
let numeric: number = 123;
let booleano: boolean = true;
let day: Date = new Date();

let anything;
anything = message;
anything = numeric;
anything = booleano;
anything = day;

let spiderman = {
    name: 'Petter',
    age: 30
};

spiderman = {
    name: 'Juan',
    age: 40,
}

export default {}